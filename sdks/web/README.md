# web-device-risk-signals

Planned browser SDK for transparent, permission-conscious raw web observations. It will expose only
browser-available data, keep network transport outside the SDK, and omit unavailable values.

It will not attempt to imitate Android/iOS probes, fingerprint users across reinstalls, or silently
request permissions. `web-device-risk-signals` is not published yet.
