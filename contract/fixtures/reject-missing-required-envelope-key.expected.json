{
  "fixture": "reject-missing-required-envelope-key.json",
  "pins": "Every required envelope key must be present; an event without collected_at cannot be ordered or aged downstream.",
  "expect": "invalid",
  "rejected_by": "schema",
  "expect_message_contains": "missing required property \"collected_at\""
}
